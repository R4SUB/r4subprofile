## Submission

This is an update of r4subprofile from 0.1.0 (on CRAN) to 0.2.0, a feature
release for the R4SUB (Ready for Submission) ecosystem. It adds versioned,
source-cited regulatory rule packs: `rule_packs()`, `rule_pack()`,
`rule_pack_indicators()`, and `rule_pack_provenance()`. See NEWS.md.

## Test environments

* local: Windows 11 x64, R 4.5.x
* GitHub Actions: ubuntu-latest, windows-latest, macos-latest (R release)

## R CMD check results

0 errors | 0 warnings | 0 notes

## Reverse dependencies

r4subprofile is imported by the r4sub meta-package and by r4subui, and suggested
by r4subpharma. Changes are additive and existing interfaces are unchanged.
