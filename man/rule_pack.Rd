% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule_packs.R
\name{rule_pack}
\alias{rule_pack}
\alias{print.rule_pack}
\title{Select a Rule Pack}
\usage{
rule_pack(id, sources = NULL)

\method{print}{rule_pack}(x, ...)
}
\arguments{
\item{id}{A pack id from \code{\link[=rule_packs]{rule_packs()}}, for example \code{"fda-2026.1"}.}

\item{sources}{Optional named character vector mapping \code{indicator_id} to a
citation, used to fill the \code{source} field. Names not in the pack are
ignored with a warning.}

\item{x}{A \code{rule_pack}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"rule_pack"} with \code{id}, \code{authority}, \code{version},
\code{effective_date}, and an \code{indicators} tibble (\code{indicator_id}, \code{source}).
}
\description{
Returns a rule pack by id (which encodes the authority and version, for
example \code{"fda-2026.1"}). Each indicator in the pack carries a \code{source} field
for the authoritative citation that motivates it.
}
\details{
The built-in packs ship with \code{source} set to \code{NA}: the citations are pending
entry by someone with the regulatory references to hand, deliberately rather
than filled with unverified section numbers. Supply them with the \code{sources}
argument, or fill them from your own controlled reference list. This keeps the
pack mechanism, the versioning, and the provenance usable now, with the
citations added when they can be stated accurately.
}
\examples{
pack <- rule_pack("fda-2026.1")
pack
rule_pack_indicators(pack)

# Supply citations for a controlled reference list:
pack <- rule_pack("fda-2026.1",
  sources = c("Q-MISS-VAR" = "FDA Study Data Technical Conformance Guide"))

}
\seealso{
\code{\link[=rule_packs]{rule_packs()}}, \code{\link[=rule_pack_provenance]{rule_pack_provenance()}}.
}
